Generated with the built-in image-generation tool. This is a conceptual architecture view, not a packet-level sequence diagram. The bridge performs request/response operations with GitHub and the coordinator; the observer is armed before execution and inspects the filesystem after termination. See the technical panel guide for the complete lifecycle.

Generate a clean WHITE opaque landscape technical flowchart, presentation-ready. Simple readable flat vector appearance, navy text, teal main arrows, blue outlines, generous whitespace. Exact title "Ephemeral Runner Cleanup Receipts". Subtitle "Execution → Observation → Signed evidence → Verification". Use THREE HORIZONTAL LANES with explicit labeled containers and no overlapping arrows. Top lane is "OPERATOR LAPTOP + GITHUB": box "Demo launcher / Bash • Python • gh" -> box "GitHub Actions / Run ID • attempt • commit" -> box "Restricted GitHub bridge / Token stays on laptop". Downward dashed line from bridge to middle-lane coordinator labelled "SSM broker / scoped API operations".
MIDDLE LANE bounded rectangle labelled "AWS EC2 — trusted host". Exactly these five left-to-right nodes inside: (1) "Coordinator / Go + client-go" (2) "kind: Kubernetes in Docker", containing TWO DISTINCT stacked inner boxes "JOB: GitHub runner + TypeScript guard / proofctl (Go): begin → inventory → cleanup → preliminary evidence" and "OBSERVER (outside job) / Logs + stopped-runner filesystem / Inspect before pod disposal"; (3) "Finalizer / Go • restricted Docker / PASS | FAIL | PARTIAL" (4) "Private Sigstore / Issuer • Fulcio • CTLog / Rekor • TSA • Cosign" (5) "Verification API / Go + Chi", with small "PostgreSQL / metadata + incidents" connected ONLY to API below it and small "Dashboard / React + TypeScript / Vite • Tailwind • TanStack" connected ONLY to API above it. Main arrows coordinator -> job labeled "Provision"; job -> observer "After termination"; observer -> finalizer "Observations"; coordinator -> finalizer via line above kind "Identity + disposal checks"; finalizer <-> Sigstore "Signing". No arrow from Sigstore to API necessary.
BOTTOM LANE is clearly OUTSIDE EC2 rectangle and labelled "AWS MANAGED STORAGE". One box "Amazon S3 + KMS / Evidence • receipt • signature bundle / Exact versions • SHA-256 • encryption • retention lock". Connect finalizer DOWN to S3 "Archive"; connect S3 UP to API "Fetch + verify". NO link S3 to PostgreSQL. Optional left bottom small operator note "Laptop browser reaches dashboard through SSM port forwarding".
Footer exact "Trust: host, observer, coordinator and private signing infrastructure. No Firecracker or physical-erasure claim." Second footer "A valid signature can authenticate a failure receipt. Cleanup verdict and application result are separate."
No giant AWS region box. Only EC2 lane is boxed so S3 absolutely outsideEC2. All text short and correct, avoid extra phrases. This is a simplified conceptual flow: intermediary queues and delivery plumbing omitted. Include a small note "Logical flow; supporting transport omitted". Do not add unrequested technologies or icons that imply microVMs.
