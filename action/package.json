{
  "name": "@cleanup-receipt/guard",
  "version": "0.1.0",
  "private": true,
  "description": "Node 24 cleanup guard, shared GitHub and local lifecycle",
  "type": "module",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "build": "tsc -p tsconfig.json && node build.mjs",
    "test": "node --test test/*.test.mjs",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@actions/artifact": "6.2.1",
    "@actions/core": "3.0.1"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3",
    "esbuild": "0.28.2"
  }
}
